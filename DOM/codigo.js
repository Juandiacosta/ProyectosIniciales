var route = window.location;
console.log(route.pathname);
document.write("Estas en: " + route.pathname);

var explora = navigator.appVersion; //Muestra la versión del navegador
console.log(explora);
document.write("Veamos que sorpresa encontramos " + explora);

var documentando = document; //Muestra las partes del código en html
console.log(documentando);
document.write("Veamos " + documentando);